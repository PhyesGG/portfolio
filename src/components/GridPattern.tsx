/* TRAME : le motif de fond vit desormais sur <body> (globals.css).
   Ce composant est neutralise pour supprimer le moire grille + trame. */
function GridPattern(_props: Record<string, unknown>) {
  return null;
}
export default GridPattern;
export { GridPattern };
